```{=html}
<% for (const item of items) { %>
<section class="<%= item.classname %>" style="padding-bottom: 2rem;">
<div class="container">

    <h2 class="section-title">
        <%= item.title %>
    </h2>
    <p class="section-description">
        <%= item.description %>
    </p>

    <%
      const layoutMap = {
        "card-with-icon": "grid-cards",
        "card-read-more": "grid-cards",
        "resource-item": "resource-list"
      };
      const layoutClass = layoutMap[item.card_style] || "grid-cards";
    %>

    <div class="<%= layoutClass %>">
        <% for (const entry of item.entries) { %>
            <%= include(item.card_style + ".ejs", entry) %>
        <% } %>
    </div>
</div>
</section>
<% } %>
```